name: Publish to PyPI

# Publishes to PyPI via OIDC Trusted Publishing (no API token stored).
# Fires when you publish a GitHub Release; can also be run manually.
on:
  release:
    types: [published]
  workflow_dispatch:

# Opt JS actions (checkout, setup-python) into the Node 24 runtime ahead of the
# Node 20 deprecation (forced 2026-06-16, removed 2026-09-16).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/graphify-swift-edges
    permissions:
      id-token: write          # required for Trusted Publishing (OIDC)
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Build sdist + wheel
        run: |
          python -m pip install --upgrade build
          python -m build

      - name: Verify metadata
        run: |
          python -m pip install --upgrade twine
          python -m twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
